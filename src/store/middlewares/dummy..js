// export const dummyMiddleWare = (store) => (next) => (action) => {
//     console.log(`The current state is ${JSON.stringify(store.getState())}`);
//     console.log(`The action is ${JSON.stringify(action)}`)
//     next(action)
// }